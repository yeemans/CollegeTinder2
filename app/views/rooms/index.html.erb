<h3> Messaging <%= get_username(@user.email) %> </h3>
<p> Messages: <%= @messages.count %> </p>
<%= @single_room.inspect %>

<%= turbo_stream_from @single_room %>
<div id="messages">
  <%= render @messages %>
</div>
<%= render partial: 'layouts/new_message_form' %>